// Alerts domain module
// Would implement lifecycle management for alerts (create, triage, escalate, close).

export function getAlertStatistics() {
  return {
    open: 3,
    escalated: 1,
    closedToday: 2,
  };
}

